spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ReaderDB?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: mipa
    password: mipa
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
logging:
  level:
    org:
      root: DEBUG
      springframework:
        security: DEBUG
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

    com:
      mipa:
        ReaderRD:
          Security:
            JwtAuthenticationFilter: DEBUG


my-settings:
  file-separator: "/"
  secret: "mipa-secret:mipamipamiaowakuwakupikaqiu"
data:
  settings:
    data-net:
      host: "http://10.12.57.22:8080"
    avatars:
      srcDir: "/statics/avatars"
      dstDir: "D:/mipa/files/avatars"
    book-cover-img:
      srcDir: "/statics/bookCoverImgs"
      dstDir: "D:/mipa/files/bookCoverImgs"
